<div>
  <h1 class="font-bold text-2xl mb-3">Editing Task</h1>

  <turbo-frame id="<%= dom_id @assignment %>">
    <%= render "form", assignment: @assignment %>
    <%= link_to "Never Mind", assignments_path, class: "btn mb-3 bg-gray-100" %>
  </turbo-frame>
</div>